import 'package:flutter/material.dart';

class DinasPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Dinas')),
      body: const Center(child: Text('Halaman Dinas')),
    );
  }
}