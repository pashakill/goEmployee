import 'package:flutter/material.dart';

class CutiPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Cuti')),
      body: const Center(child: Text('Halaman Cuti')),
    );
  }
}