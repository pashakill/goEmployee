import 'package:flutter/material.dart';

class WfhPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('WFH')),
      body: const Center(child: Text('Halaman WFH')),
    );
  }
}